import {css} from 'element-vir';

export const storyPointsClasses = css`
    .story-points {
        background: #ddd;
        font-weight: bold;
        color: #333;
        border-radius: 8px;
        padding: 1px;
        font-size: 0.8em;
        min-width: 24px;
        text-align: center;
    }
`;
